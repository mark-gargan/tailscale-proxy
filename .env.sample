# Server
LISTEN_ADDR=":80"
PRESERVE_HOST=false

# Routing rules: comma-separated rules of the form
#   /api=http://localhost:9000
#   app.example.com=http://localhost:3000
#   app.example.com/app=http://localhost:3001
# Longest path prefix wins; exact host matches first.
ROUTES=/api=http://localhost:9000,/app=http://localhost:3000

# Default backend (optional) used when no route matches
# DEFAULT_BACKEND=http://localhost:8081

# Auth mode: off | shared_secret | google | both
AUTH_MODE=off

# Shared secret auth (when AUTH_MODE includes shared_secret)
# Clients must send: Authorization: Bearer <AUTH_SHARED_SECRET>
# AUTH_SHARED_SECRET=change-me

# Google OIDC (when AUTH_MODE includes google)
# Obtain a Web app OAuth 2.0 Client ID from Google Cloud Console.
# GOOGLE_OIDC_CLIENT_ID=YOUR_CLIENT_ID.apps.googleusercontent.com

# Limit who can access (optional, comma-separated)
# ALLOWED_EMAILS=you@example.com,teammate@example.com
# ALLOWED_DOMAINS=example.com

